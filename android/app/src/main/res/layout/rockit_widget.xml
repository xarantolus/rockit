<?xml version="1.0" encoding="utf-8"?>
<!--
  RemoteViews accepts only a fixed set of views: LinearLayout, TextView and
  ImageView are fine, ConstraintLayout and a plain View are not and would fail
  at inflate time on the device rather than at build time.

  The rows are not here. The provider appends them one at a time into
  widget_rows with RemoteViews.addView, so how many exist follows the widget's
  height instead of however many were worth writing out by hand.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/widget_background"
    android:padding="@dimen/widget_padding">

    <LinearLayout
        android:id="@+id/widget_header"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingBottom="6dp">

        <!--
          The notification glyph, not the launcher icon: it is already a
          monochrome silhouette drawn to stay legible at this size, where the
          full icon turns to mush.
        -->
        <ImageView
            android:layout_width="13dp"
            android:layout_height="13dp"
            android:layout_marginEnd="6dp"
            android:src="@drawable/notification_icon"
            android:tint="@color/widget_accent"
            android:contentDescription="@null" />

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/widget_title"
            android:textColor="@color/widget_muted"
            android:textSize="11sp"
            android:letterSpacing="0.08"
            android:maxLines="1" />
    </LinearLayout>

    <TextView
        android:id="@+id/widget_empty"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textColor="@color/widget_muted"
        android:textSize="13sp"
        android:visibility="gone" />

    <!--
      The rows take the rest of the height and sit centred in it, so the space
      left over by a row that did not fit is split above and below rather than
      piling up at the bottom, where it read as the list having been cut off.
      The header stays pinned at the top, which is why this is a container of
      its own rather than a gravity on the root.
    -->
    <LinearLayout
        android:id="@+id/widget_rows"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:orientation="vertical"
        android:gravity="center_vertical" />
</LinearLayout>
